(function(R, $, undefined) {

 $("#header #total_points .point-interval").html('<%= @approval.giver.interval_points %>');
var $el = $("#recognition-approval-<%= @approval.recognition_id%>");
$el.replaceWith('<%= escape_javascript(render partial: "recognitions/recognition_approval_section", locals: {recognition: @approval.recognition, approvers_limit: params[:approvers_limit].to_i}) %>');
})(window.R, jQuery);
