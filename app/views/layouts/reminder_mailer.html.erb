<p><%= t("layouts.mailer.greetings", name: (@user.first_name.present? ? @user.first_name : t("layouts.mailer.default_name"))) %>,</p>

<%= yield%>

<%= t("layouts.mailer.thanks_so_much_recognize_html") %>

<p style="<%= mail_styles(:text, :p, :textSubtle) %> margin-bottom: 5px;"><%= t("layouts.mailer.unsubscribe_html", link: link_to(t('layouts.mailer.footer.edit_profile_page'), edit_user_url(@user), style: mail_styles(:a))) %></p>