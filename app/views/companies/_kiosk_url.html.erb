<% if @company.kiosk_mode_key && @company.kiosk_mode_key != "" %>
  <% url = kiosk_mode_url %> 
  <%= link_to url, url, target: "_blank", class: "kiosk_url_link"%>
<% else %>
  <span class="subtle-text">No url set</span>
<% end %>