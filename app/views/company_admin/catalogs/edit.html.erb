<h2><%= t("company_admin.rewards.catalog.edit_catalog") %></h2>
<%= render("form", {
  catalog: @catalog,
  company_roles: @company_roles,
  url: company_admin_catalog_path(id: @catalog.id, network: @company.domain),
  edit: true
}) %>
